# Keep GitHub Actions current so runtime (Node.js) deprecations surface as
# update PRs instead of workflow warnings.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
